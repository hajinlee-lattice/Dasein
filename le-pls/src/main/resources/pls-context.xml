<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
  http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
  http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.0.xsd
  http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.0.xsd">

    <import resource="classpath:proxy-context.xml"/>
    <import resource="classpath:db-context.xml"/>
    <import resource="classpath:security-context.xml"/>
    <import resource="classpath:pls-persistence-context.xml"/>
    <import resource="classpath:pls-aspects-context.xml"/>
    <import resource="classpath:pls-camille-context.xml"/>
    <import resource="classpath:pls-yarn-config-context.xml"/>
    <import resource="classpath:common-jmx-component-context.xml"/>
    <import resource="classpath:pls-monitor-aspects-context.xml"/>
    <import resource="classpath:monitor-alerts-context.xml"/>
    <import resource="classpath:liaison-context.xml" />
    <import resource="classpath:workflow-context.xml"/>
    <import resource="classpath:pls-quartz-${LOCAL_MODEL_DL_QUARTZ_ENABLED:disabled}-context.xml"/>

    <context:component-scan base-package="com.latticeengines.pls">
        <!-- Need to exclude instantiating the swagger config except for bringing up the API
             since the servlet container instances are not available when running tests. -->
        <context:exclude-filter type="assignable" expression="com.latticeengines.pls.swagger.SwaggerConfig" />
    </context:component-scan>

    <mvc:annotation-driven/>

    <bean id="deleteModelThreadPool" class="org.springframework.scheduling.concurrent.ThreadPoolTaskExecutor">
        <property name="corePoolSize" value="${pls.modeldeletion.core.pool.size}" />
        <property name="maxPoolSize" value="${pls.modeldeletion.max.pool.size}" />
        <property name="queueCapacity" value="${pls.modeldeletion.queue.capacity}" />
        <property name="rejectedExecutionHandler">
            <bean class="java.util.concurrent.ThreadPoolExecutor$CallerRunsPolicy" />
        </property>
    </bean>

    <bean id="modelSummaryDownloadExecutor" class="org.springframework.scheduling.concurrent.ThreadPoolTaskExecutor">
        <property name="corePoolSize" value="${pls.downloader.core.pool.size}" />
        <property name="maxPoolSize" value="${pls.downloader.max.pool.size}" />
        <property name="queueCapacity" value="${pls.downloader.queue.capacity}" />
        <property name="rejectedExecutionHandler">
            <bean class="java.util.concurrent.ThreadPoolExecutor$CallerRunsPolicy" />
        </property>
    </bean>

</beans>

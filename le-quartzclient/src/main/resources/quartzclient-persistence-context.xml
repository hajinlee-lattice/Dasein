<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:tx="http://www.springframework.org/schema/tx"
    xmlns:p="http://www.springframework.org/schema/p"
    xmlns:context="http://www.springframework.org/schema/context"
    xmlns:aop="http://www.springframework.org/schema/aop"
    xsi:schemaLocation="http://www.springframework.org/schema/beans
	    http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context-4.0.xsd
	    http://www.springframework.org/schema/tx
	    http://www.springframework.org/schema/tx/spring-tx-4.0.xsd">

    <bean id="dataSourceQuartz"
        class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="${quartzclient.datasource.driver}" />
        <property name="url" value="${quartzclient.datasource.url}" />
        <property name="username" value="${quartzclient.datasource.user}" />
        <property name="password" value="${quartzclient.datasource.password.encrypted}" />
    </bean>

    <!-- Hibernate session factory -->
    <bean id="sessionFactoryQuartz"
        class="org.springframework.orm.hibernate4.LocalSessionFactoryBean">
        <property name="dataSource" ref="dataSourceQuartz" />
        <property name="packagesToScan"
            value="com.latticeengines.domain.exposed.quartz, com.latticeengines.domain.exposed" />
        <property name="hibernateProperties">
            <props>
                <prop key="hibernate.dialect">${quartzclient.datasource.dialect}</prop>
                <prop key="hibernate.show_sql">false</prop>
                <prop key="hibernate.globally_quoted_identifiers">true</prop>
            </props>
        </property>
    </bean>

    <bean id="transactionManagerQuartz"
        class="org.springframework.orm.hibernate4.HibernateTransactionManager"
        p:sessionFactory-ref="sessionFactoryQuartz">
        <qualifier value="quartz" />
    </bean>

    <tx:annotation-driven transaction-manager="transactionManagerQuartz" />

    <bean id="jobHistoryDao" class="com.latticeengines.quartzclient.dao.impl.JobHistoryDaoImpl">
        <property name="sessionFactory" ref="sessionFactoryQuartz" />
    </bean>
    <bean id="activeStackDao" class="com.latticeengines.quartzclient.dao.impl.ActiveStackDaoImpl">
        <property name="sessionFactory" ref="sessionFactoryQuartz" />
    </bean>

</beans>

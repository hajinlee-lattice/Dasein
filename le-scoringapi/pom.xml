<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>
    <artifactId>le-scoringapi</artifactId>
    <packaging>war</packaging>
    <name>le-${component-name}</name>
    <description>LE Scoring API</description>

    <parent>
        <groupId>com.latticeengines</groupId>
        <artifactId>le-web-parent</artifactId>
        <version>4.3.1-SNAPSHOT</version>
        <relativePath>../le-parent/web-pom.xml</relativePath>
    </parent>

    <properties>
        <component-name>scoringapi</component-name>
        <le.camille.version>4.3.1-SNAPSHOT</le.camille.version>
        <le.common.version>4.3.1-SNAPSHOT</le.common.version>
        <le.domain.version>4.3.1-SNAPSHOT</le.domain.version>
        <le.datafabric.version>4.3.1-SNAPSHOT</le.datafabric.version>
        <le.monitor.version>4.3.1-SNAPSHOT</le.monitor.version>
        <le.oauth2.db.version>4.3.1-SNAPSHOT</le.oauth2.db.version>
        <le.proxy.version>4.3.1-SNAPSHOT</le.proxy.version>
        <le.network.version>4.3.1-SNAPSHOT</le.network.version>
        <le.transform.version>4.3.1-SNAPSHOT</le.transform.version>
        <le.testframework.version>4.3.1-SNAPSHOT</le.testframework.version>
        <le.transform.version>4.3.1-SNAPSHOT</le.transform.version>
        <le.hadoop.version>4.3.1-SNAPSHOT</le.hadoop.version>
        <jetty.port>8073</jetty.port>
        <jett.stop.port>10973</jett.stop.port>
    </properties>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <executions>
                    <execution>
                        <id>make-a-jar</id>
                        <phase>compile</phase>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-install-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>install</phase>
                        <goals>
                            <goal>install-file</goal>
                        </goals>
                        <configuration>
                            <packaging>jar</packaging>
                            <artifactId>${project.artifactId}</artifactId>
                            <groupId>${project.parent.groupId}</groupId>
                            <version>${project.parent.version}</version>
                            <file>
                                ${project.build.directory}/${project.artifactId}-${project.parent.version}.jar
                            </file>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-deploy-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>deploy</phase>
                        <goals>
                            <goal>deploy-file</goal>
                        </goals>
                        <configuration>
                            <packaging>jar</packaging>
                            <generatePom>true</generatePom>
                            <url>${project.distributionManagement.repository.url}</url>
                            <artifactId>${project.artifactId}</artifactId>
                            <groupId>${project.parent.groupId}</groupId>
                            <version>${project.parent.version}</version>
                            <file>${project.build.directory}/${project.artifactId}-${project.parent.version}.jar</file>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>com.github.kongchen</groupId>
                <artifactId>swagger-maven-plugin</artifactId>
                <version>3.1.6</version>
                <configuration>
                    <apiSources>
                      <apiSource>
                            <springmvc>true</springmvc>
                            <locations>
                                <location>com.latticeengines.scoringapi.controller</location>
                            </locations>
                            <schemes>https</schemes>
                            <!--  <host>api.lattice-engines.com</host> -->
                            <basePath>/score</basePath>
                            <info>
                                <title>Swagger Documentation for Lattice Scoring API</title>
                                <version>v1</version>
                                <description>Swagger Documentation for Lattice Scoring API</description>
                                <termsOfService>
                                    http://lattice-engines.com
                                </termsOfService>
                                <contact>
                                    <url>http://lattice-engines.com </url>
                                </contact>
                            </info>
                            <!-- 
                            <securityDefinitions>
	                            <securityDefinition>
	                                <type>apiKey</type>
	                                <name>Authorization</name>
	                                <in>header</in>
	                            </securityDefinition>
                            </securityDefinitions>
                            
                            <templatePath>${basedir}/templates/strapdown.html.hbs</templatePath>
                            <outputPath>${basedir}/target/swagger-gen/document.html</outputPath>
                             -->
                            <swaggerDirectory>target/swagger-gen</swaggerDirectory>
                        </apiSource>
                    </apiSources>
                </configuration>
                <executions>
                    <execution>
                        <phase>compile</phase>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <dependencies>
        <dependency>
            <groupId>com.latticeengines</groupId>
            <artifactId>le-common-web</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>com.latticeengines</groupId>
            <artifactId>le-baton</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>com.latticeengines</groupId>
            <artifactId>le-transform</artifactId>
            <version>${le.transform.version}</version>
        </dependency>

        <dependency>
            <groupId>com.latticeengines</groupId>
            <artifactId>le-domain</artifactId>
            <version>${le.domain.version}</version>
            <classifier>intf</classifier>
        </dependency>

        <dependency>
            <groupId>com.latticeengines</groupId>
            <artifactId>le-monitor</artifactId>
            <version>${le.monitor.version}</version>
            <classifier>intf</classifier>
        </dependency>

        <dependency>
            <groupId>com.latticeengines</groupId>
            <artifactId>le-monitor</artifactId>
            <version>${le.monitor.version}</version>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>com.latticeengines</groupId>
            <artifactId>le-monitor</artifactId>
            <version>${le.monitor.version}</version>
            <classifier>tests</classifier>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>com.latticeengines</groupId>
            <artifactId>le-oauth2db</artifactId>
            <version>${le.oauth2.db.version}</version>
            <exclusions>
                <exclusion>
                    <artifactId>spring-boot-starter-jetty</artifactId>
                    <groupId>org.springframework.boot</groupId>
                </exclusion>
                <exclusion>
                    <artifactId>spring-boot-starter-web</artifactId>
                    <groupId>org.springframework.boot</groupId>
                </exclusion>
                <exclusion>
                    <artifactId>
                        spring-boot-starter-actuator
                    </artifactId>
                    <groupId>org.springframework.boot</groupId>
                </exclusion>
                <exclusion>
                    <artifactId>spring-boot-starter-jdbc</artifactId>
                    <groupId>org.springframework.boot</groupId>
                </exclusion>
                <exclusion>
                    <artifactId>
                        spring-boot-starter-security
                    </artifactId>
                    <groupId>org.springframework.boot</groupId>
                </exclusion>
                <exclusion>
                    <groupId>org.springframework.security</groupId>
                    <artifactId>spring-security-web</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <dependency>
            <groupId>com.latticeengines</groupId>
            <artifactId>le-proxy</artifactId>
            <version>${le.proxy.version}</version>
        </dependency>

        <dependency>
            <groupId>com.latticeengines</groupId>
            <artifactId>le-datafabric</artifactId>
            <version>${le.datafabric.version}</version>
            <exclusions>
                <exclusion>
                    <groupId>io.confluent</groupId>
                    <artifactId>kafka-connect-hdfs</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <dependency>
            <groupId>com.latticeengines</groupId>
            <artifactId>le-testframework</artifactId>
            <version>${project.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.jpmml</groupId>
            <artifactId>pmml-evaluator</artifactId>
            <version>${jpmml.version}</version>
        </dependency>

        <dependency>
            <groupId>javax.servlet</groupId>
            <artifactId>jstl</artifactId>
            <version>1.2</version>
        </dependency>

        <dependency>
            <groupId>com.latticeengines</groupId>
            <artifactId>le-network</artifactId>
            <version>${le.network.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>com.latticeengines</groupId>
            <artifactId>le-hadoop</artifactId>
            <version>${le.hadoop.version}</version>
            <scope>runtime</scope>
        </dependency>

    </dependencies>

</project>

<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>
    <artifactId>le-serviceflows</artifactId>
    <packaging>pom</packaging>
    <name>le-${component-name}</name>

    <properties>
        <component-name>serviceflows</component-name>
        <le.camille.version>3.4.0-SNAPSHOT</le.camille.version>
        <le.dataflow.version>3.4.0-SNAPSHOT</le.dataflow.version>
        <le.domain.version>3.4.0-SNAPSHOT</le.domain.version>
        <le.serviceflows.core.version>3.4.0-SNAPSHOT</le.serviceflows.core.version>
        <le.swlib.version>3.4.0-SNAPSHOT</le.swlib.version>
        <le.workflow.version>3.4.0-SNAPSHOT</le.workflow.version>
    </properties>


    <parent>
        <groupId>com.latticeengines</groupId>
        <artifactId>le-parent</artifactId>
        <version>3.4.0-SNAPSHOT</version>
        <relativePath>../le-parent</relativePath>
    </parent>

    <dependencies>
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-csv</artifactId>
            <version>1.2</version>
        </dependency>
    </dependencies>

    <modules>
        <module>core</module>
        <module>datacloud</module>
        <module>prospectdiscovery</module>
        <module>leadprioritization</module>
        <module>cdl</module>
    </modules>

    <build>
        <plugins>
            <!-- Override parent pom to exclude svn revision number.  
                 For some reason it's failing for certain developers. -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>buildnumber-maven-plugin</artifactId>
                <configuration>
                   <skip>true</skip>
                </configuration>
            </plugin>
       </plugins>
    </build>


    <!-- Override parent functional profile in order to always run in local mode -->
    <profiles>
        <profile>
            <!-- A profile to run on the hadoop job runner-->
            <id>functional-cluster</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <version>${maven.surefire.version}</version>
                        <executions>
                            <execution>
                                <id>unit-test</id>
                                <configuration>
                                    <skip>true</skip>
                                </configuration>
                            </execution>
                            <execution>
                                <id>default-test</id>
                                <configuration>
                                    <skip>true</skip>
                                </configuration>
                            </execution>
                            <execution>
                                <id>functional-test</id>
                                <goals>
                                    <goal>test</goal>
                                </goals>
                                <phase>integration-test</phase>
                                <configuration>
                                    <argLine>${maxpermsize} ${argLine} -Dsqoop.throwOnError=true</argLine>
                                    <skip>false</skip>
                                    <includes>
                                        <include>${functional.classes.include}</include>
                                    </includes>
                                    <excludes>
                                        <exclude>${functional.classes.exclude}</exclude>
                                        <exclude>${deployment.classes.include}</exclude>
                                        <exclude>${infrastructure.classes.include}</exclude>
                                    </excludes>
                                    <groups>${functional.groups}</groups>
                                    <forkMode>always</forkMode>
                                    <additionalClasspathElements>
                                        <additionalClasspathElement>${env.HADOOP_HOME}/etc/hadoop
                                        </additionalClasspathElement>
                                        <additionalClasspathElement>${env.TEZ_CONF_DIR}</additionalClasspathElement>
                                    </additionalClasspathElements>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

    </profiles>

</project>



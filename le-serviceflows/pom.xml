<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>
    <artifactId>le-serviceflows</artifactId>
    <packaging>pom</packaging>
    <name>le-${component-name}</name>

    <properties>
        <component-name>serviceflows</component-name>
        <le.dataflow.version>2.0.16-SNAPSHOT</le.dataflow.version>
<!--        <le.workflow.version>2.0.16-SNAPSHOT</le.workflow.version> -->
        <le.domain.version>2.0.16-SNAPSHOT</le.domain.version>
        <le.serviceflows.core.version>2.0.16-SNAPSHOT</le.serviceflows.core.version>
        <le.swlib.version>2.0.16-SNAPSHOT</le.swlib.version>
    </properties>


    <parent>
        <groupId>com.latticeengines</groupId>
        <artifactId>le-parent</artifactId>
        <version>2.0.16-SNAPSHOT</version>
        <relativePath>../le-parent</relativePath>
    </parent>


    <modules>
        <module>core</module>
        <module>prospectdiscovery</module>
    </modules>

    <!-- Override parent functional profile in order to always run in local mode -->
    <profiles>
        <profile>
            <id>functional</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <version>${maven.surefire.version}</version>
                        <executions>
                            <execution>
                                <id>unit-test</id>
                                <configuration>
                                    <skip>true</skip>
                                </configuration>
                            </execution>
                            <execution>
                                <id>default-test</id>
                                <configuration>
                                    <skip>true</skip>
                                </configuration>
                            </execution>
                            <execution>
                                <id>functional-test</id>
                                <goals>
                                    <goal>test</goal>
                                </goals>
                                <phase>integration-test</phase>
                                <configuration combine.self="override">
                                    <argLine>${maxpermsize} ${argLine} -Dsqoop.throwOnError=true</argLine>
                                    <skip>false</skip>
                                    <includes>
                                        <include>${functional.classes.include}</include>
                                    </includes>
                                    <excludes>
                                        <exclude>${functional.classes.exclude}</exclude>
                                        <exclude>${deployment.classes.include}</exclude>
                                        <exclude>${infrastructure.classes.include}</exclude>
                                    </excludes>
                                    <groups>${functional.groups}</groups>
                                    <forkMode>always</forkMode>
                                    <classpathDependencyExcludes>
                                        <classpathDependencyExcludes>${env.HADOOP_HOME}/etc/hadoop</classpathDependencyExcludes>
                                    </classpathDependencyExcludes>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>


</project>



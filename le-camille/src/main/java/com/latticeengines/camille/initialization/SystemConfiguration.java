package com.latticeengines.camille.initialization;

/**
 * Initializes a system that has already been bootstrapped (has a Pod ID, etc.)
 */
public class SystemConfiguration extends Initializer {
	public SystemConfiguration() { }
}

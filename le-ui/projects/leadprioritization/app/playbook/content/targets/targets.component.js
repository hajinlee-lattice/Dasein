angular.module('lp.playbook.wizard.targets', [])
.controller('PlaybookWizardTargets', function(
    $state, $stateParams, ResourceUtility, PlaybookWizardStore
) {
    var vm = this;

    angular.extend(vm, {
    });

    vm.init = function() {

    };

    vm.init();
});
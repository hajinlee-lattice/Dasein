angular.module('controllers.jobs', [
    'controllers.jobs.import.credentials',
    'controllers.jobs.import.file',
    'controllers.jobs.status'
])

.controller('JobsCtrl', function($scope, $rootScope) {
    
});
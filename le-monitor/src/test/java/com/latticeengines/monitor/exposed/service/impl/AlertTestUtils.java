package com.latticeengines.monitor.exposed.service.impl;

import static org.testng.Assert.assertTrue;

public class AlertTestUtils {

    public static void confirmAlertIncident(boolean flag) {
        assertTrue(flag);
    }
	
}
<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd">

    <bean id="emailSettings" class="com.latticeengines.domain.exposed.monitor.EmailSettings">
        <property name="from" value="${monitor.emailsettings.from:no-reply@lattice-engines.com}" />
        <property name="server" value="${monitor.emailsettings.server:smtprelay.lattice.local}" />
        <property name="username" value="${monitor.emailsettings.username:build@lattice-engines.com}" />
        <property name="password" value="${monitor.emailsettings.password:ryTy2wEiaw51Le9qTXxx3w==}" />
        <property name="port" value="${monitor.emailsettings.port:25}" />
        <property name="useSSL" value="${monitor.emailsettings.useSSL:false}" />
        <property name="useSTARTTLS" value="${monitor.emailsettings.useSTARTTLS:false}" />
    </bean>

    <bean id="sendgridEmailSettings" class="com.latticeengines.domain.exposed.monitor.EmailSettings">
        <property name="from" value="${monitor.sendgrid.emailsettings.from:no-reply@connect.dnb.com}" />
        <property name="server" value="${monitor.sendgrid.emailsettings.from.server:smtp.sendgrid.net}" />
        <property name="username" value="${monitor.sendgrid.emailsettings.from.username:apikey}" />
        <property name="password" value="${monitor.sendgrid.emailsettings.from.password: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}" />
        <property name="port" value="${monitor.sendgrid.emailsettings.port:25}" />
        <property name="useSSL" value="${monitor.sendgrid.emailsettings.useSSL:false}" />
        <property name="useSTARTTLS" value="${monitor.sendgrid.emailsettings.useSTARTTLS:false}" />
    </bean>

</beans>

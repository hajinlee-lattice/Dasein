<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:p="http://www.springframework.org/schema/p"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
      http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
      http://www.springframework.org/schema/tx
      http://www.springframework.org/schema/tx/spring-tx-4.0.xsd">



    <!-- Hibernate data source -->
    <bean id="dataSourceDellEbiTarget"
        class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="${dellebi.datatarget.driver}" />
        <property name="url" value="${dellebi.datatarget.url}" />
        <property name="username" value="${dellebi.datatarget.user}" />
        <property name="password" value="${dellebi.datatarget.password.encrypted}" />
    </bean>

    <!-- Hibernate data source -->
    <bean id="dataSourceDellEbi"
        class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="${dellebi.configdb.datasource.driver}" />
        <property name="url" value="${dellebi.configdb.datasource.url}" />
        <property name="username" value="${dellebi.configdb.datasource.user}" />
        <property name="password"
            value="${dellebi.configdb.datasource.password.encrypted}" />
    </bean>

    <!-- Hibernate session factory -->
    <!--  
    <bean id="sessionFactoryDellEbi"
        class="org.springframework.orm.hibernate4.LocalSessionFactoryBean">
        <property name="dataSource" ref="dataSourceDellEbi" />
        <property name="packagesToScan" value="com.latticeengines.domain.exposed.dellebi" />
        <property name="hibernateProperties">
            <props>
                <prop key="hibernate.dialect">${dellebi.configdb.datasource.dialect}</prop>
                <prop key="hibernate.show_sql">false</prop>
                <prop key="hibernate.globally_quoted_identifiers">true</prop>
            </props>
        </property>
    </bean>
	-->
	
	<!-- Hibernate session factory -->
    <bean id="sessionFactoryDellEbi" class="org.springframework.orm.jpa.vendor.HibernateJpaSessionFactoryBean">
	    <property name="entityManagerFactory" ref="entityManagerFactoryDellEbi" />
	</bean>
	
	<!-- Entity manager factory -->
	<bean id="entityManagerFactoryDellEbi" 
			class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean">
        <property name="dataSource" ref="dataSourceDellEbi" />
        <property name="packagesToScan" value="com.latticeengines.domain.exposed.dellebi" />
		<property name="jpaVendorAdapter">
			<bean class="org.springframework.orm.jpa.vendor.HibernateJpaVendorAdapter">
				<!-- property name="prepareConnection" value="false"/-->
			</bean>
		</property>
		<property name="jpaProperties">
          <props>
            <prop key="hibernate.dialect">${dellebi.configdb.datasource.dialect}</prop>
            <prop key="hibernate.show_sql">false</prop>
            <prop key="hibernate.globally_quoted_identifiers">true</prop>
            <prop key="hibernate.id.new_generator_mappings">true</prop>
            <prop key="hibernate.current_session_context_class">org.springframework.orm.hibernate5.SpringSessionContext</prop>
            <prop key="hibernate.allow_refresh_detached_entity">true</prop>
            <!-- prop key="hibernate.connection.release_mode">on_close</prop-->
            <!-- prop key="hibernate.connection.handling_mode">DELAYED_ACQUISITION_AND_RELEASE_AFTER_TRANSACTION</prop-->
          </props>
      </property>
	</bean>
		
    <bean id="transactionManagerDellEbiCfg"
        class="org.springframework.orm.hibernate5.HibernateTransactionManager"
        p:sessionFactory-ref="sessionFactoryDellEbi">
        <qualifier value="dataSourceDellEbiTarget"/>
    </bean>

    <tx:annotation-driven transaction-manager="transactionManagerDellEbiCfg"/>

    <bean id="dellEbiTargetJDBCTemplate" class="org.springframework.jdbc.core.JdbcTemplate"
        p:dataSource-ref="dataSourceDellEbiTarget" />

    <bean id="dellEbiConfigDao"
        class="com.latticeengines.dellebi.dao.impl.DellEbiConfigDaoImpl">
        <property name="sessionFactory" ref="sessionFactoryDellEbi" />
    </bean>

    <bean id="dellEbiExecutionLogDao"
        class="com.latticeengines.dellebi.dao.impl.DellEbiExecutionLogDaoImpl">
        <property name="sessionFactory" ref="sessionFactoryDellEbi" />
    </bean>

</beans>
<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:security="http://www.springframework.org/schema/security"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
       http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-4.2.xsd
       http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <import resource="classpath:security-api-context.xml"/>
    <import resource="classpath:common-testclient-env-context.xml"/>
    <import resource="classpath:security-provider-context.xml"/>

    <mvc:annotation-driven/>

    <security:http use-expressions="true"
                   pattern="/*/users/**"
                   entry-point-ref="restAuthenticationEntryPoint"
                   create-session="stateless">
        <security:csrf disabled="true"/>
        <security:custom-filter ref="restServicesFilter" before="PRE_AUTH_FILTER" />
    </security:http>

    <security:http auto-config="false"
                   use-expressions="true"
                   pattern="/security/adresource/**"
                   entry-point-ref="restAuthenticationEntryPoint"
                   create-session="stateless">
        <security:csrf disabled="true"/>
        <security:custom-filter ref="restActiveDirectoryServicesFilter" before="PRE_AUTH_FILTER" />
    </security:http>

    <security:http auto-config="false"
                   use-expressions="true"
                   pattern="/*/garesource/**"
                   entry-point-ref="restAuthenticationEntryPoint"
                   create-session="stateless">
        <security:csrf disabled="true"/>
        <security:custom-filter ref="restServicesFilter" before="PRE_AUTH_FILTER" />
    </security:http>

    <security:http auto-config="false"
                   use-expressions="true"
                   pattern="/security/internal/**"
                   entry-point-ref="restAuthenticationEntryPoint"
                   create-session="stateless">
        <security:csrf disabled="true"/>
        <security:intercept-url pattern="/security/internal/health/**" access="permitAll" />
        <security:intercept-url pattern="/security/internal/**" access="hasRole('INTERNAL_USER')" />
        <security:custom-filter ref="internalRestServicesFilter" before="PRE_AUTH_FILTER" />
    </security:http>

</beans>
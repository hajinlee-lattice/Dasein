<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans-3.0.xsd">

    <bean name="scoringManagerJob"
        class="org.springframework.scheduling.quartz.JobDetailFactoryBean">
        <property name="durability" value="true" />
        <property name="jobClass"
            value="com.latticeengines.scoring.orchestration.service.impl.ScoringManagerServiceImpl" />
        <property name="jobDataAsMap">
            <map>
                <entry key="customerBaseDir" value="${dataplatform.customer.basedir}" />
                <entry key="cleanUpInterval" value="#{T(java.lang.Double).parseDouble(${scoring.cleanup.timeinternval})}" />
                <entry key="enableCleanHdfs" value="#{T(java.lang.Boolean).parseBoolean(${scoring.cleanup.enableCleanHdfs})}" />
            </map>
        </property>
    </bean>

    <!-- Cron Trigger, run every 15 seconds -->
    <bean id="scoringManagerCronTrigger"
        class="org.springframework.scheduling.quartz.CronTriggerFactoryBean">
        <property name="jobDetail" ref="scoringManagerJob" />
        <property name="cronExpression" value="0/15 * * * * ?" />
    </bean>

    <bean class="org.springframework.scheduling.quartz.SchedulerFactoryBean">
        <property name="triggers">
            <list>
                <ref bean="scoringManagerCronTrigger" />
            </list>
        </property>
        <property name="applicationContextSchedulerContextKey">
            <value>applicationContext</value>
        </property>
    </bean>
</beans>
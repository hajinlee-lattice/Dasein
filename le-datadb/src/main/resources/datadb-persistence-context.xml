<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:p="http://www.springframework.org/schema/p"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
      http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
      http://www.springframework.org/schema/tx
      http://www.springframework.org/schema/tx/spring-tx-4.0.xsd">

    <bean name="dataDbSource" class="com.mchange.v2.c3p0.ComboPooledDataSource" destroy-method="close">
        <property name="driverClass" value="${datadb.datasource.driver}"/>
        <property name="jdbcUrl" value="${datadb.datasource.url}"/>
        <property name="user" value="${datadb.datasource.user}" />
        <property name="password" value="${datadb.datasource.password.encrypted}" />
        <property name="acquireIncrement" value="1"/>
        <property name="minPoolSize" value="2"/>
        <property name="maxPoolSize" value="8"/>
        <property name="checkoutTimeout" value="10000"/>
        <property name="idleConnectionTestPeriod" value="10"/>
        <property name="maxIdleTime" value="30"/>
    </bean>

    <!-- Hibernate session factory -->
    <bean id="dataSessionFactory"
        class="org.springframework.orm.hibernate4.LocalSessionFactoryBean">
        <property name="dataSource" ref="dataDbSource" />
        <property name="packagesToScan" value="com.latticeengines.domain.exposed" />
        <property name="hibernateProperties">
            <props>
                <prop key="hibernate.dialect">${datadb.datasource.dialect}</prop>
                <prop key="hibernate.show_sql">${datadb.datasource.show_sql:false}</prop>
                <prop key="hibernate.globally_quoted_identifiers">true</prop>
            </props>
        </property>
    </bean>

    <bean id="dataTransactionManager"
        class="org.springframework.orm.hibernate4.HibernateTransactionManager"
        p:sessionFactory-ref="dataSessionFactory">
    </bean>

    <tx:annotation-driven transaction-manager="dataTransactionManager" />

    <bean id="recommendationDao" class="com.latticeengines.datadb.dao.impl.RecommendationDaoImpl">
        <property name="sessionFactory" ref="dataSessionFactory"/>
    </bean>

    <bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate"
        p:dataSource-ref="dataDbSource" />

</beans>
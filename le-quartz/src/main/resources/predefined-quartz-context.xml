<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:util="http://www.springframework.org/schema/util"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
    http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util-3.0.xsd">

    <bean name="lpModelSummaryDownload" class="com.latticeengines.domain.exposed.quartz.JobConfig">
        <property name="jobName" value="modelSummaryDownload"/>
        <property name="cronTrigger" value="${quartz.lp.modelsummarydownload.job.cron}"/>
        <property name="destUrl" value="${quartz.lp.predefined.job.primary.url}"/>
        <property name="secondaryDestUrl" value="${quartz.lp.predefined.job.secondary.url}"/>
        <property name="queryApi" value="${quartz.lp.predefined.job.query.api}"/>
        <property name="checkJobBeanUrl" value="${quartz.lp.predefined.job.check.bean.url}"/>
        <property name="jobArguments" value="{&quot;jobType&quot;:&quot;modelSummaryDownload&quot;}"/>
        <property name="jobTimeout" value="${quartz.lp.modelsummarydownload.job.timeout}"/>
    </bean>

    <bean name="lpModelSummaryFullDownload" class="com.latticeengines.domain.exposed.quartz.JobConfig">
        <property name="jobName" value="modelSummaryFullDownload"/>
        <property name="cronTrigger" value="${quartz.lp.modelsummaryfulldownload.job.cron:0 0/20 * * * ?}"/>
        <property name="destUrl" value="${quartz.lp.predefined.job.primary.url:}"/>
        <property name="secondaryDestUrl" value="${quartz.lp.predefined.job.secondary.url:}"/>
        <property name="queryApi" value="${quartz.lp.predefined.job.query.api:}"/>
        <property name="checkJobBeanUrl" value="${quartz.lp.predefined.job.check.bean.url:}"/>
        <property name="jobArguments" value="{&quot;jobType&quot;:&quot;modelSummaryFullDownload&quot;}"/>
        <property name="jobTimeout" value="${quartz.lp.modelsummaryfulldownload.job.timeout:3600}"/>
    </bean>

    <bean name="lpEmrScaling" class="com.latticeengines.domain.exposed.quartz.JobConfig">
        <property name="jobName" value="emrScaling"/>
        <property name="cronTrigger" value="${quartz.lp.emrscaling.job.cron}"/>
        <property name="destUrl" value="${quartz.lp.predefined.job.primary.url}"/>
        <property name="secondaryDestUrl" value="${quartz.lp.predefined.job.secondary.url}"/>
        <property name="queryApi" value="${quartz.lp.predefined.job.query.api}"/>
        <property name="checkJobBeanUrl" value="${quartz.lp.predefined.job.check.bean.url}"/>
        <property name="jobArguments" value="{&quot;jobType&quot;:&quot;emrScalingBean&quot;}"/>
        <property name="jobTimeout" value="${quartz.lp.emrscaling.job.timeout:3600}"/>
    </bean>

    <bean name="plsSegmentExportCleanupJob" class="com.latticeengines.domain.exposed.quartz.JobConfig">
        <property name="jobName" value="segmentExportCleanupJob"/>
        <property name="cronTrigger" value="${quartz.pls.segmentexportcleanup.job.cron:0 0 */6 * * ?}"/>
        <property name="destUrl" value="${quartz.pls.predefined.job.primary.url:}"/>
        <property name="secondaryDestUrl" value="${quartz.pls.predefined.job.secondary.url:}"/>
        <property name="queryApi" value="${quartz.pls.predefined.job.query.api:}"/>
        <property name="checkJobBeanUrl" value="${quartz.pls.predefined.job.check.bean.url:}"/>
        <property name="jobArguments" value="{&quot;jobType&quot;:&quot;segmentExportCleanupJob&quot;}"/>
        <property name="jobTimeout" value="${quartz.pls.segmentexportcleanup.job.timeout:3600}"/>
    </bean>

    <bean name="playmakerRecommendationCleanupJob" class="com.latticeengines.domain.exposed.quartz.JobConfig">
        <property name="jobName" value="recommendationCleanupJob"/>
        <property name="cronTrigger" value="${quartz.playmaker.recommendationcleanup.job.cron:0 */2 * * * ?}"/>
        <property name="destUrl" value="${quartz.playmaker.predefined.job.primary.url:}"/>
        <property name="secondaryDestUrl" value="${quartz.playmaker.predefined.job.secondary.url:}"/>
        <property name="queryApi" value="${quartz.playmaker.predefined.job.query.api:}"/>
        <property name="checkJobBeanUrl" value="${quartz.playmaker.predefined.job.check.bean.url:}"/>
        <property name="jobArguments" value="{&quot;jobType&quot;:&quot;recommendationCleanupJob&quot;}"/>
        <property name="jobTimeout" value="${quartz.playmaker.recommendationcleanup.job.timeout:3600}"/>
    </bean>

    <bean name="dataCloudRefresh" class="com.latticeengines.domain.exposed.quartz.JobConfig">
        <property name="jobName" value="dataCloudRefresh"/>
        <property name="cronTrigger" value="${quartz.datacloudapi.refresh.job.cron:*/10 * * * * ?}"/>
        <property name="destUrl" value="${quartz.datacloudapi.predefined.job.primary.url:}"/>
        <property name="secondaryDestUrl" value="${quartz.datacloudapi.predefined.job.secondary.url:}"/>
        <property name="queryApi" value="${quartz.datacloudapi.predefined.job.query.api:}"/>
        <property name="checkJobBeanUrl" value="${quartz.datacloudapi.predefined.job.check.bean.url:}"/>
        <property name="jobArguments" value="{&quot;jobType&quot;:&quot;dataCloudRefresh&quot;}"/>
        <property name="jobTimeout" value="${quartz.datacloudapi.refresh.job.timeout:3600}"/>
    </bean>

    <bean name="cdlImportJob" class="com.latticeengines.domain.exposed.quartz.JobConfig">
        <property name="jobName" value="cdlImportJob"/>
        <property name="cronTrigger" value="${quartz.cdl.import.job.cron}"/>
        <property name="destUrl" value="${quartz.cdl.predefined.job.primary.url}"/>
        <property name="secondaryDestUrl" value="${quartz.cdl.predefined.job.secondary.url}"/>
        <property name="queryApi" value="${quartz.cdl.predefined.job.query.api}"/>
        <property name="checkJobBeanUrl" value="${quartz.cdl.predefined.job.check.bean.url}"/>
        <property name="jobArguments" value="{&quot;jobType&quot;:&quot;cdlImportJob&quot;}"/>
        <property name="jobTimeout" value="${quartz.cdl.import.job.timeout}"/>
    </bean>

    <bean name="cdlOrchestrationJob" class="com.latticeengines.domain.exposed.quartz.JobConfig">
        <property name="jobName" value="cdlOrchestrationJob"/>
        <property name="cronTrigger" value="${quartz.cdl.orchestration.job.cron:0 0/10 * * * ?}"/>
        <property name="destUrl" value="${quartz.cdl.predefined.job.primary.url:}"/>
        <property name="secondaryDestUrl" value="${quartz.cdl.predefined.job.secondary.url:}"/>
        <property name="queryApi" value="${quartz.cdl.predefined.job.query.api:}"/>
        <property name="checkJobBeanUrl" value="${quartz.cdl.predefined.job.check.bean.url:}"/>
        <property name="jobArguments" value="{&quot;jobType&quot;:&quot;cdlOrchestrationJob&quot;}"/>
        <property name="jobTimeout" value="${quartz.cdl.profile.job.timeout:3600}"/>
    </bean>

    <bean name="cdlDateFeedExecutionCleanupJob" class="com.latticeengines.domain.exposed.quartz.JobConfig">
        <property name="jobName" value="cdlDateFeedExecutionCleanupJob"/>
        <property name="cronTrigger" value="${quartz.cdl.execution.cleanup.job.cron:0 0/30 * * * ?}"/>
        <property name="destUrl" value="${quartz.cdl.predefined.job.primary.url:}"/>
        <property name="secondaryDestUrl" value="${quartz.cdl.predefined.job.secondary.url:}"/>
        <property name="queryApi" value="${quartz.cdl.predefined.job.query.api:}"/>
        <property name="checkJobBeanUrl" value="${quartz.cdl.predefined.job.check.bean.url:}"/>
        <property name="jobArguments" value="{&quot;jobType&quot;:&quot;cdlDateFeedExecutionCleanupJob&quot;}"/>
        <property name="jobTimeout" value="${quartz.cdl.profile.job.timeout:3600}"/>
    </bean>

    <bean name="cdlRedShiftCleanupJob" class="com.latticeengines.domain.exposed.quartz.JobConfig">
        <property name="jobName" value="cdlRedShiftCleanupJob"/>
        <property name="cronTrigger" value="${quartz.cdl.execution.cleanup.job.cron:0 0 3 * * ?}"/>
        <property name="destUrl" value="${quartz.cdl.predefined.job.primary.url:}"/>
        <property name="secondaryDestUrl" value="${quartz.cdl.predefined.job.secondary.url:}"/>
        <property name="queryApi" value="${quartz.cdl.predefined.job.query.api:}"/>
        <property name="checkJobBeanUrl" value="${quartz.cdl.predefined.job.check.bean.url:}"/>
        <property name="jobArguments" value="{&quot;jobType&quot;:&quot;cdlRedShiftCleanupJob&quot;}"/>
        <property name="jobTimeout" value="${quartz.cdl.profile.job.timeout:3600}"/>
    </bean>

    <bean name="cdlExportScheduleJob" class="com.latticeengines.domain.exposed.quartz.JobConfig">
        <property name="jobName" value="cdlExportScheduleJob"/>
        <property name="cronTrigger" value="${quartz.cdl.execution.export.job.cron}"/>
        <property name="destUrl" value="${quartz.cdl.predefined.job.primary.url:}"/>
        <property name="secondaryDestUrl" value="${quartz.cdl.predefined.job.secondary.url:}"/>
        <property name="queryApi" value="${quartz.cdl.predefined.job.query.api:}"/>
        <property name="checkJobBeanUrl" value="${quartz.cdl.predefined.job.check.bean.url:}"/>
        <property name="jobArguments" value="{&quot;jobType&quot;:&quot;cdlExportScheduleJob&quot;}"/>
        <property name="jobTimeout" value="${quartz.cdl.export.job.timeout}"/>
    </bean>

    <bean name="cdlSchedulingPAJob" class="com.latticeengines.domain.exposed.quartz.JobConfig">
        <property name="jobName" value="cdlSchedulingPAJob"/>
        <property name="cronTrigger" value="${quartz.cdl.execution.schedulingPA.job.cron}"/>
        <property name="destUrl" value="${quartz.cdl.predefined.job.primary.url:}"/>
        <property name="secondaryDestUrl" value="${quartz.cdl.predefined.job.secondary.url:}"/>
        <property name="queryApi" value="${quartz.cdl.predefined.job.query.api:}"/>
        <property name="checkJobBeanUrl" value="${quartz.cdl.predefined.job.check.bean.url:}"/>
        <property name="jobArguments" value="{&quot;jobType&quot;:&quot;cdlSchedulingPAJob&quot;}"/>
        <property name="jobTimeout" value="${quartz.cdl.schedulingPA.job.timeout}"/>
    </bean>

    <bean name="cdlSchedulingNowPAJob" class="com.latticeengines.domain.exposed.quartz.JobConfig">
        <property name="jobName" value="cdlSchedulingNowPAJob"/>
        <property name="cronTrigger" value="${quartz.cdl.execution.schedulingNowPA.job.cron}"/>
        <property name="destUrl" value="${quartz.cdl.predefined.job.primary.url:}"/>
        <property name="secondaryDestUrl" value="${quartz.cdl.predefined.job.secondary.url:}"/>
        <property name="queryApi" value="${quartz.cdl.predefined.job.query.api:}"/>
        <property name="checkJobBeanUrl" value="${quartz.cdl.predefined.job.check.bean.url:}"/>
        <property name="jobArguments" value="{&quot;jobType&quot;:&quot;cdlSchedulingNowPAJob&quot;}"/>
        <property name="jobTimeout" value="${quartz.cdl.schedulingNowPA.job.timeout}"/>
    </bean>

    <bean name="cdlEntityStateCorrectionJob" class="com.latticeengines.domain.exposed.quartz.JobConfig">
        <property name="jobName" value="cdlEntityStateCorrectionJob"/>
        <property name="cronTrigger" value="${quartz.cdl.entity.state.correction.job.cron}"/>
        <property name="destUrl" value="${quartz.cdl.predefined.job.primary.url:}"/>
        <property name="secondaryDestUrl" value="${quartz.cdl.predefined.job.secondary.url:}"/>
        <property name="queryApi" value="${quartz.cdl.predefined.job.query.api:}"/>
        <property name="checkJobBeanUrl" value="${quartz.cdl.predefined.job.check.bean.url:}"/>
        <property name="jobArguments" value="{&quot;jobType&quot;:&quot;cdlEntityStateCorrectionJob&quot;}"/>
        <property name="jobTimeout" value="${quartz.cdl.export.job.timeout}"/>
    </bean>

    <bean name="cdlCampaignLaunchSchedulerJob" class="com.latticeengines.domain.exposed.quartz.JobConfig">
        <property name="jobName" value="cdlCampaignLaunchSchedulerJob"/>
        <property name="cronTrigger" value="${quartz.cdl.campaign.launch.scheduler.job.cron}"/>
        <property name="destUrl" value="${quartz.cdl.predefined.job.primary.url:}"/>
        <property name="secondaryDestUrl" value="${quartz.cdl.predefined.job.secondary.url:}"/>
        <property name="queryApi" value="${quartz.cdl.predefined.job.query.api:}"/>
        <property name="checkJobBeanUrl" value="${quartz.cdl.predefined.job.check.bean.url:}"/>
        <property name="jobArguments" value="{&quot;jobType&quot;:&quot;cdlCampaignLaunchSchedulerJob&quot;}"/>
        <property name="jobTimeout" value="${quartz.cdl.export.job.timeout}"/>
    </bean>

    <bean name="cdlSendDNBIntentAlertEmailJob" class="com.latticeengines.domain.exposed.quartz.JobConfig">
        <property name="jobName" value="cdlSendDNBIntentAlertEmailJob"/>
        <property name="cronTrigger" value="${quartz.cdl.execution.sendDNBIntentAlertEmail.job.cron}"/>
        <property name="destUrl" value="${quartz.cdl.predefined.job.primary.url:}"/>
        <property name="secondaryDestUrl" value="${quartz.cdl.predefined.job.secondary.url:}"/>
        <property name="queryApi" value="${quartz.cdl.predefined.job.query.api:}"/>
        <property name="checkJobBeanUrl" value="${quartz.cdl.predefined.job.check.bean.url:}"/>
        <property name="jobArguments" value="{&quot;jobType&quot;:&quot;cdlSendDNBIntentAlertEmailJob&quot;}"/>
        <property name="jobTimeout" value="${quartz.cdl.sendDNBIntentAlertEmail.job.timeout}"/>
    </bean>

    <bean name="datacloudCollectionJob" class="com.latticeengines.domain.exposed.quartz.JobConfig">
        <property name="jobName" value="datacloudCollectionJob"/>
        <property name="cronTrigger" value="${quartz.datacloudcollection.job.cron:0/30 * * * * ?}"/>
        <property name="destUrl" value="${quartz.datacloudapi.predefined.job.primary.url:}"/>
        <property name="secondaryDestUrl" value="${quartz.datacloudapi.predefined.job.secondary.url:}"/>
        <property name="queryApi" value="${quartz.datacloudapi.predefined.job.query.api:}"/>
        <property name="checkJobBeanUrl" value="${quartz.datacloudapi.predefined.job.check.bean.url:}"/>
        <property name="jobArguments" value="{&quot;jobType&quot;:&quot;datacloudCollectionJob&quot;}"/>
        <property name="jobTimeout" value="${quartz.datacloudcollection.job.timeout}"/>
    </bean>

    <bean name="adminRecycleTenantJob" class="com.latticeengines.domain.exposed.quartz.JobConfig">
        <property name="jobName" value="adminRecycleTenantJob"/>
        <property name="cronTrigger" value="${quartz.admin.recycletenant.job.cron:0 0 12 * * ?}"/>
        <property name="destUrl" value="${quartz.admin.predefined.job.primary.url:}"/>
        <property name="secondaryDestUrl" value="${quartz.admin.predefined.job.secondary.url:}"/>
        <property name="queryApi" value="${quartz.admin.predefined.job.query.api:}"/>
        <property name="checkJobBeanUrl" value="${quartz.admin.predefined.job.check.bean.url:}"/>
        <property name="jobArguments" value="{&quot;jobType&quot;:&quot;adminRecycleTenantJob&quot;}"/>
        <property name="jobTimeout" value="${quartz.admin.recycletenant.job.timeout:3000}"/>
    </bean>

    <bean name="adminCleanupErrorTenantJob" class="com.latticeengines.domain.exposed.quartz.JobConfig">
        <property name="jobName" value="adminCleanupErrorTenantJob"/>
        <property name="cronTrigger" value="${quartz.admin.cleanuptenant.job.cron:0 0 2 * * ?}"/>
        <property name="destUrl" value="${quartz.admin.predefined.job.primary.url:}"/>
        <property name="secondaryDestUrl" value="${quartz.admin.predefined.job.secondary.url:}"/>
        <property name="queryApi" value="${quartz.admin.predefined.job.query.api:}"/>
        <property name="checkJobBeanUrl" value="${quartz.admin.predefined.job.check.bean.url:}"/>
        <property name="jobArguments" value="{&quot;jobType&quot;:&quot;adminCleanupErrorTenantJob&quot;}"/>
        <property name="jobTimeout" value="${quartz.admin.cleanuptenant.job.timeout:3000}"/>
    </bean>

    <bean name="workflowJobStatusChangeJob" class="com.latticeengines.domain.exposed.quartz.JobConfig">
        <property name="jobName" value="workflowJobStatusChangeJob"/>
        <property name="cronTrigger" value="${quartz.workflowapi.statuschange.job.cron}"/>
        <property name="destUrl" value="${quartz.workflowapi.predefined.job.primary.url}"/>
        <property name="secondaryDestUrl" value="${quartz.workflowapi.predefined.job.secondary.url}"/>
        <property name="queryApi" value="${quartz.workflowapi.predefined.job.query.api}"/>
        <property name="checkJobBeanUrl" value="${quartz.workflowapi.predefined.job.check.bean.url}"/>
        <property name="jobArguments" value="{&quot;jobType&quot;:&quot;workflowJobStatusChangeJob&quot;}"/>
        <property name="jobTimeout" value="${quartz.workflowapi.statuschange.job.timeout}"/>
    </bean>

    <bean name="metadataTableCleanupJob" class="com.latticeengines.domain.exposed.quartz.JobConfig">
        <property name="jobName" value="metadataTableCleanupJob"/>
        <property name="cronTrigger" value="${quartz.metadata.execution.tablecleanup.job.cron:0 0 2 * * ?}"/>
        <property name="destUrl" value="${quartz.metadata.predefined.job.primary.url:}"/>
        <property name="secondaryDestUrl" value="${quartz.metadata.predefined.job.secondary.url:}"/>
        <property name="queryApi" value="${quartz.metadata.predefined.job.query.api:}"/>
        <property name="checkJobBeanUrl" value="${quartz.metadata.predefined.job.check.bean.url:}"/>
        <property name="jobArguments" value="{&quot;jobType&quot;:&quot;metadataTableCleanupJob&quot;}"/>
        <property name="jobTimeout" value="${quartz.metadata.tablecleanup.job.timeout:3600}"/>
    </bean>

    <bean name="metadataMigrateDynamoJob" class="com.latticeengines.domain.exposed.quartz.JobConfig">
        <property name="jobName" value="metadataMigrateDynamoJob"/>
        <property name="cronTrigger" value="${quartz.metadata.execution.migratedynamo.job.cron:0 0 0 * * ?}"/>
        <property name="destUrl" value="${quartz.metadata.predefined.job.primary.url:}"/>
        <property name="secondaryDestUrl" value="${quartz.metadata.predefined.job.secondary.url:}"/>
        <property name="queryApi" value="${quartz.metadata.predefined.job.query.api:}"/>
        <property name="checkJobBeanUrl" value="${quartz.metadata.predefined.job.check.bean.url:}"/>
        <property name="jobArguments" value="{&quot;jobType&quot;:&quot;metadataMigrateDynamoJob&quot;}"/>
        <property name="jobTimeout" value="${quartz.metadata.migratedynamo.job.timeout:3600}"/>
    </bean>

    <bean name="dcpRollupDataReportJob" class="com.latticeengines.domain.exposed.quartz.JobConfig">
        <property name="jobName" value="dcpRollupDataReportJob"/>
        <property name="cronTrigger" value="${quartz.dcp.execution.rollupReport.job.cron:0 */10 * * * ?}"/>
        <property name="destUrl" value="${quartz.dcp.predefined.job.primary.url:}"/>
        <property name="secondaryDestUrl" value="${quartz.dcp.predefined.job.secondary.url:}"/>
        <property name="queryApi" value="${quartz.dcp.predefined.job.query.api:}"/>
        <property name="checkJobBeanUrl" value="${quartz.dcp.predefined.job.check.bean.url:}"/>
        <property name="jobArguments" value="{&quot;jobType&quot;:&quot;dcpRollupDataReportJob&quot;}"/>
        <property name="jobTimeout" value="${quartz.dcp.rollupReport.job.timeout:3600}"/>
    </bean>

    <bean name="submitVboUsageReportJob" class="com.latticeengines.domain.exposed.quartz.JobConfig">
        <property name="jobName" value="submitVboUsageReportJob"/>
        <property name="cronTrigger" value="${quartz.matchapi.execution.vboUsageReport.job.cron}"/>
        <property name="destUrl" value="${quartz.matchapi.predefined.job.primary.url}"/>
        <property name="secondaryDestUrl" value="${quartz.matchapi.predefined.job.secondary.url}"/>
        <property name="queryApi" value="${quartz.matchapi.predefined.job.query.api}"/>
        <property name="checkJobBeanUrl" value="${quartz.matchapi.predefined.job.check.bean.url}"/>
        <property name="jobArguments" value="{&quot;jobType&quot;:&quot;submitVboUsageReportJob&quot;}"/>
        <property name="jobTimeout" value="${quartz.matchapi.vboUsageReport.job.timeout:3600}"/>
    </bean>

    <bean name="plsActivityAlertsCleanupJob" class="com.latticeengines.domain.exposed.quartz.JobConfig">
        <property name="jobName" value="alertCleanupJob"/>
        <property name="cronTrigger" value="${quartz.pls.altercleanup.job.cron:0 0 0 1/2 * ? *}"/>
        <property name="destUrl" value="${quartz.pls.predefined.job.primary.url:}"/>
        <property name="secondaryDestUrl" value="${quartz.pls.predefined.job.secondary.url:}"/>
        <property name="queryApi" value="${quartz.pls.predefined.job.query.api:}"/>
        <property name="checkJobBeanUrl" value="${quartz.pls.predefined.job.check.bean.url:}"/>
        <property name="jobArguments" value="{&quot;jobType&quot;:&quot;alertCleanupJob&quot;}"/>
        <property name="jobTimeout" value="${quartz.pls.alertcleanup.job.timeout:3600}"/>
    </bean>

    <bean name="cdlMockBrokerJob" class="com.latticeengines.domain.exposed.quartz.JobConfig">
        <property name="jobName" value="cdlMockBrokerJob"/>
        <property name="cronTrigger" value="${quartz.cdl.mock.broker.job.cron}"/>
        <property name="destUrl" value="${quartz.cdl.predefined.job.primary.url}"/>
        <property name="secondaryDestUrl" value="${quartz.cdl.predefined.job.secondary.url}"/>
        <property name="queryApi" value="${quartz.cdl.predefined.job.query.api}"/>
        <property name="checkJobBeanUrl" value="${quartz.cdl.predefined.job.check.bean.url}"/>
        <property name="jobArguments" value="{&quot;jobType&quot;:&quot;cdlMockBrokerJob&quot;}"/>
        <property name="jobTimeout" value="${quartz.cdl.mock.broker.job.timeout}"/>
    </bean>

    <util:list id="predefinedJobs" list-class="java.util.ArrayList">
        <ref bean="lpModelSummaryFullDownload"/>
        <ref bean="lpModelSummaryDownload"/>
        <ref bean="lpEmrScaling"/>
        <ref bean="plsSegmentExportCleanupJob"/>
        <ref bean="plsActivityAlertsCleanupJob"/>
        <ref bean="playmakerRecommendationCleanupJob"/>
        <ref bean="dataCloudRefresh"/>
        <ref bean="datacloudCollectionJob"/>
        <ref bean="cdlImportJob"/>
        <ref bean="cdlOrchestrationJob"/>
        <ref bean="cdlDateFeedExecutionCleanupJob"/>
        <ref bean="cdlRedShiftCleanupJob"/>
        <ref bean="cdlExportScheduleJob"/>
        <ref bean="cdlSchedulingPAJob"/>
        <ref bean="cdlSchedulingNowPAJob"/>
        <ref bean="cdlEntityStateCorrectionJob"/>
        <ref bean="cdlCampaignLaunchSchedulerJob"/>
        <ref bean="cdlSendDNBIntentAlertEmailJob"/>
        <ref bean="adminRecycleTenantJob"/>
        <ref bean="adminCleanupErrorTenantJob"/>
        <ref bean="workflowJobStatusChangeJob"/>
        <ref bean="metadataTableCleanupJob"/>
        <ref bean="metadataMigrateDynamoJob"/>
        <ref bean="dcpRollupDataReportJob"/>
        <ref bean="submitVboUsageReportJob"/>
        <ref bean="cdlMockBrokerJob"/>
    </util:list>

</beans>

<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>
    <artifactId>le-web-parent</artifactId>
    <packaging>pom</packaging>
    <version>4.13.0-SNAPSHOT</version>
    <description>Parent of all lattice engine web projects.</description>

    <parent>
        <groupId>com.latticeengines</groupId>
        <artifactId>le-parent</artifactId>
        <version>4.13.0-SNAPSHOT</version>
        <relativePath>pom.xml</relativePath>
    </parent>

    <properties>
        <jetty.dir>main</jetty.dir>
        <jetty.conf.dir>../le-parent/jetty</jetty.conf.dir>
        <jetty.ssl.port>9080</jetty.ssl.port>
        <jetty.stop.port>10999</jetty.stop.port>
        <jetty.use.test.cp>false</jetty.use.test.cp>
        <jetty.ks.path>/etc/ledp/tls/ledp_keystore.jks</jetty.ks.path>
        <jetty.ks.password>Lattice1</jetty.ks.password>
        <jetty.xmx>2g</jetty.xmx>
        <alpn.version>8.1.11.v20170118</alpn.version>
    </properties>

    <profiles>
        <profile>
            <id>jetty</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.eclipse.jetty</groupId>
                        <artifactId>jetty-maven-plugin</artifactId>
                        <version>${jetty.version}</version>
                        <configuration>
                            <jettyXml>
                                ${jetty.conf.dir}/jetty.xml,
                                ${jetty.conf.dir}/jetty-ssl.xml,
                                ${jetty.conf.dir}/jetty-ssl-context.xml,
                                ${jetty.conf.dir}/jetty-https.xml
                            </jettyXml>
                            <httpConnector>
                                <port>${jetty.port}</port>
                            </httpConnector>
                            <webAppConfig>
                                <extraClasspath>${env.HADOOP_CONF_DIR};${env.JAVA_HOME}/lib/tools.jar
                                </extraClasspath>
                            </webAppConfig>
                            <systemProperties>
                                <systemProperty>
                                    <name>log4j.configuration</name>
                                    <value>file:///${project.basedir}/src/${jetty.dir}/resources/log4j.properties</value>
                                </systemProperty>
                                <systemProperty>
                                    <name>com.latticeengines.registerBootstrappers</name>
                                    <value>true</value>
                                </systemProperty>
                                <systemProperty>
                                    <name>jdk.tls.ephemeralDHKeySize</name>
                                    <value>2048</value>
                                </systemProperty>
                                <systemProperty>
                                    <name>jetty.ssl.port</name>
                                    <value>${jetty.ssl.port}</value>
                                </systemProperty>
                                <systemProperty>
                                    <name>jetty.sslContext.keyStorePath</name>
                                    <value>${jetty.ks.path}</value>
                                </systemProperty>
                                <systemProperty>
                                    <name>jetty.sslContext.keyStorePassword</name>
                                    <value>${jetty.ks.password}</value>
                                </systemProperty>
                                <systemProperty>
                                    <name>org.eclipse.jetty.annotations.maxWait</name>
                                    <value>300</value>
                                </systemProperty>
                            </systemProperties>
                            <webXml>${basedir}/src/${jetty.dir}/webapp/WEB-INF/web.xml</webXml>
                            <webAppSourceDirectory>${basedir}/src/${jetty.dir}/webapp</webAppSourceDirectory>
                            <useTestClasspath>${jetty.use.test.cp}</useTestClasspath>
                            <scanIntervalSeconds>0</scanIntervalSeconds>
                            <stopPort>${jetty.stop.port}</stopPort>
                            <stopKey>STOP</stopKey>
                            <stopWait>10</stopWait>
                            <daemon>true</daemon>
                            </configuration>
                        <executions>
                            <execution>
                                <id>start-jetty</id>
                                <phase>pre-integration-test</phase>
                                <goals>
                                    <goal>start</goal>
                                </goals>
                                <configuration>
                                    <scanIntervalSeconds>0</scanIntervalSeconds>
                                    <daemon>true</daemon>
                                    <waitForChild>false</waitForChild>
                                    <jvmArgs>${jacoco.agent.itArgLine} -Xmx${jetty.xmx}</jvmArgs>
                                </configuration>
                            </execution>
                            <execution>
                                <id>stop-jetty</id>
                                <phase>post-integration-test</phase>
                                <goals>
                                    <goal>stop</goal>
                                </goals>
                            </execution>
                        </executions>
                        <dependencies>
                            <dependency>
                                <groupId>com.sun.mail</groupId>
                                <artifactId>javax.mail</artifactId>
                                <version>${javax.mail.version}</version>
                            </dependency>
                        </dependencies>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>jetty2</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-dependency-plugin</artifactId>
                        <executions>
                            <!-- Copy the ant tasks jar. Needed for ts.jacoco.report-ant . -->
                            <execution>
                                <id>download-alpn-boot</id>
                                <goals>
                                    <goal>copy</goal>
                                </goals>
                                <phase>generate-sources</phase>
                                <configuration>
                                    <artifactItems>
                                        <artifactItem>
                                            <groupId>org.mortbay.jetty.alpn</groupId>
                                            <artifactId>alpn-boot</artifactId>
                                            <version>${alpn.version}</version>
                                        </artifactItem>
                                    </artifactItems>
                                    <stripVersion>true</stripVersion>
                                    <outputDirectory>${basedir}/target/alpn-boot</outputDirectory>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.eclipse.jetty</groupId>
                        <artifactId>jetty-maven-plugin</artifactId>
                        <version>${jetty.version}</version>
                        <configuration>
                            <jettyXml>
                                ${jetty.conf.dir}/jetty.xml,
                                ${jetty.conf.dir}/jetty-ssl.xml,
                                ${jetty.conf.dir}/jetty-ssl-context.xml,
                                ${jetty.conf.dir}/jetty-alpn.xml,
                                ${jetty.conf.dir}/jetty-http2.xml,
                                ${jetty.conf.dir}/jetty-https.xml
                            </jettyXml>
                            <webAppConfig>
                                <extraClasspath>${env.HADOOP_CONF_DIR};${env.JAVA_HOME}/lib/tools.jar
                                </extraClasspath>
                            </webAppConfig>
                            <systemProperties>
                                <systemProperty>
                                    <name>log4j.configuration</name>
                                    <value>file:///${project.basedir}/src/${jetty.dir}/resources/log4j.properties</value>
                                </systemProperty>
                                <systemProperty>
                                    <name>com.latticeengines.registerBootstrappers</name>
                                    <value>true</value>
                                </systemProperty>
                                <systemProperty>
                                    <name>jdk.tls.ephemeralDHKeySize</name>
                                    <value>2048</value>
                                </systemProperty>
                                <systemProperty>
                                    <name>jetty.httpConfig.securePort</name>
                                    <value>${jetty.ssl.port}</value>
                                </systemProperty>
                                <systemProperty>
                                    <name>jetty.ssl.port</name>
                                    <value>${jetty.ssl.port}</value>
                                </systemProperty>
                                <systemProperty>
                                    <name>jetty.sslContext.keyStorePath</name>
                                    <value>${jetty.ks.path}</value>
                                </systemProperty>
                                <systemProperty>
                                    <name>jetty.sslContext.keyStorePassword</name>
                                    <value>${jetty.ks.password}</value>
                                </systemProperty>
                            </systemProperties>
                            <jvmArgs>
                                -Xbootclasspath/p:${basedir}/target/alpn-boot/alpn-boot.jar
                            </jvmArgs>
                            <webXml>${basedir}/src/${jetty.dir}/webapp/WEB-INF/web.xml</webXml>
                            <webAppSourceDirectory>${basedir}/src/${jetty.dir}/webapp</webAppSourceDirectory>
                            <useTestClasspath>${jetty.use.test.cp}</useTestClasspath>
                            <scanIntervalSeconds>0</scanIntervalSeconds>
                            <stopPort>${jetty.stop.port}</stopPort>
                            <stopKey>STOP</stopKey>
                            <stopWait>10</stopWait>
                            <daemon>true</daemon>
                        </configuration>
                        <dependencies>
                            <dependency>
                                <groupId>com.sun.mail</groupId>
                                <artifactId>javax.mail</artifactId>
                                <version>${javax.mail.version}</version>
                            </dependency>
                            <dependency>
                                <groupId>org.eclipse.jetty</groupId>
                                <artifactId>jetty-servlets</artifactId>
                                <version>${jetty.version}</version>
                            </dependency>
                            <dependency>
                                <groupId>org.eclipse.jetty</groupId>
                                <artifactId>jetty-alpn-server</artifactId>
                                <version>${jetty.version}</version>
                            </dependency>
                            <dependency>
                                <groupId>org.eclipse.jetty.http2</groupId>
                                <artifactId>http2-server</artifactId>
                                <version>${jetty.version}</version>
                            </dependency>
                        </dependencies>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>swagger-json</id>
            <build>
                <plugins>
                    <plugin>
		                <groupId>com.github.kongchen</groupId>
		                <artifactId>swagger-maven-plugin</artifactId>
		                <version>3.1.6</version>
		                <configuration>
		                    <apiSources>
		                      <apiSource>
		                            <springmvc>true</springmvc>
		                            <locations>
		                                <location>com.latticeengines.scoringapi.controller</location>
		                            </locations>
		                            <schemes>https</schemes>
		                            <!--  <host>api.lattice-engines.com</host> -->
		                            <basePath>/score</basePath>
		                            <info>
		                                <title>Swagger Documentation for Lattice Scoring API</title>
		                                <version>v1</version>
		                                <description>Swagger Documentation for Lattice Scoring API</description>
		                                <termsOfService>
		                                    http://lattice-engines.com
		                                </termsOfService>
		                                <contact>
		                                    <url>http://lattice-engines.com </url>
		                                </contact>
		                            </info>
		                            <!--
		                            <securityDefinitions>
		                                <securityDefinition>
		                                    <type>apiKey</type>
		                                    <name>Authorization</name>
		                                    <in>header</in>
		                                </securityDefinition>
		                            </securityDefinitions>
		                            -->
		                            <templatePath>${basedir}/templates/strapdown.html.hbs</templatePath>
		                            <outputPath>${basedir}/target/swagger-gen/document.html</outputPath>

		                            <swaggerDirectory>target/swagger-gen</swaggerDirectory>
		                        </apiSource>
		                    </apiSources>
		                </configuration>
		                <executions>
		                    <execution>
		                        <phase>compile</phase>
		                        <goals>
		                            <goal>generate</goal>
		                        </goals>
		                    </execution>
		                </executions>
		            </plugin>
                </plugins>
            </build>
         </profile>
    </profiles>


    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <version>${maven.war.version}</version>
                <configuration>
                    <archive>
                        <manifestEntries>
                            <LE-SCM-Revision>${buildNumber}</LE-SCM-Revision>
                        </manifestEntries>
                    </archive>
                </configuration>
            </plugin>
        </plugins>

        <defaultGoal>install</defaultGoal>
        <pluginManagement>
            <plugins>
                <!-- The Jetty Maven plugin is useful for rapid development and testing. You can add
                    it to any webapp project that is structured according to the usual Maven defaults. The plugin can then
                    periodically scan your project for changes and automatically redeploy the webapp if any are found. This
                    makes the development cycle more productive by eliminating the build and deploy steps: you use your IDE
                    to make changes to the project, and the running web container automatically picks them up, allowing you
                    to test them straight away. To run - mvn jetty:run -->
                <plugin>
                    <groupId>org.eclipse.jetty</groupId>
                    <artifactId>jetty-maven-plugin</artifactId>
                    <version>${jetty.version}</version>
                    <executions>
                        <execution>
                            <id>start-jetty</id>
                            <phase>process-test-classes</phase>
                            <goals>
                                <goal>start</goal>
                            </goals>
                            <configuration>
                                <scanIntervalSeconds>10</scanIntervalSeconds>
                                <daemon>true</daemon>
                            </configuration>
                        </execution>
                        <execution>
                            <id>stop-jetty</id>
                            <phase>post-integration-test</phase>
                            <goals>
                                <goal>stop</goal>
                            </goals>
                            <configuration>
                                <stopPort>9001</stopPort>
                                <stopKey>secret</stopKey>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <dependencies>
        <dependency>
            <groupId>io.springfox</groupId>
            <artifactId>springfox-swagger-ui</artifactId>
        </dependency>

        <dependency>
            <groupId>io.springfox</groupId>
            <artifactId>springfox-swagger2</artifactId>
        </dependency>

        <dependency>
            <groupId>javax.annotation</groupId>
            <artifactId>jsr250-api</artifactId>
        </dependency>

        <dependency>
            <groupId>javax.servlet</groupId>
            <artifactId>javax.servlet-api</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>javax.servlet.jsp</groupId>
            <artifactId>jsp-api</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- Spring Framework Dependencies -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-webmvc</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-webflux</artifactId>
        </dependency>


    </dependencies>

</project>

<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>
    <artifactId>le-microservice-parent</artifactId>
    <packaging>pom</packaging>
    <name>le-${component-name}</name>

    <properties>
        <component-name>microservice-parent</component-name>
        <le.monitor.version>4.17.0-SNAPSHOT</le.monitor.version>
        <le.microservice.version>4.17.0-SNAPSHOT</le.microservice.version>
        <le.propdata.version>4.17.0-SNAPSHOT</le.propdata.version>
        <le.network.version>4.17.0-SNAPSHOT</le.network.version>
        <le.quartz.version>4.17.0-SNAPSHOT</le.quartz.version>
        <le.modelquality.version>4.17.0-SNAPSHOT</le.modelquality.version>
        <jetty.port>8080</jetty.port>
        <jetty.ssl.port>9080</jetty.ssl.port>
        <jett.stop.port>10980</jett.stop.port>
        <jetty.dir>test</jetty.dir>
        <jetty.use.test.cp>true</jetty.use.test.cp>
    </properties>

    <parent>
        <groupId>com.latticeengines</groupId>
        <artifactId>le-spring-boot-tomcat-parent</artifactId>
        <version>4.17.0-SNAPSHOT</version>
        <relativePath>../le-parent/spring-boot/tomcat</relativePath>
    </parent>

    <dependencies>
        <dependency>
            <groupId>com.latticeengines</groupId>
            <artifactId>le-common-web</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.latticeengines</groupId>
            <artifactId>le-monitor</artifactId>
            <version>${project.version}</version>
            <classifier>intf</classifier>
        </dependency>
        <dependency>
            <groupId>com.latticeengines</groupId>
            <artifactId>le-monitor</artifactId>
            <version>${project.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>com.latticeengines</groupId>
            <artifactId>le-network</artifactId>
            <version>${project.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>com.latticeengines</groupId>
            <artifactId>le-testframework</artifactId>
            <version>${project.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.latticeengines</groupId>
            <artifactId>le-monitor</artifactId>
            <version>${project.version}</version>
            <classifier>tests</classifier>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <modules>
        <module>web-pom.xml</module>
        <module>core</module>
        <module>eai</module>
        <module>metadata</module>
        <module>modeling</module>
        <module>dataflowapi</module>
        <module>datacloudapi</module>
        <module>scoring</module>
        <module>workflowapi</module>
        <module>dellebi</module>
        <module>quartz</module>
        <module>modelquality</module>
        <module>sqoop</module>
        <module>objectapi</module>
        <module>cdl</module>
        <module>lp</module>
    </modules>

</project>
